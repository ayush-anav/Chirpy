# Middleware
![middleware](/Lesson%20Breakdown/pics/middleware.png)

### Yay: http.HandlerFunc() will take your callback function and convert it into a type that satisfies the http.Handler interface. 

This is the most important bit because of our return type **http.Handler interface**

### IF you do not make use of next.ServeHTTP(), then you will block forever, it wont go forward in its chain 

#### including your main business logic (the final handler), never gets executed.